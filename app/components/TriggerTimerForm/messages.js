/*
 * TriggerTimerForm Messages
 *
 * This contains all the text for the TriggerTimerForm component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  headerhttptrigger: {
    id: 'app.components.TriggerTimerForm.headerhttptrigger',
    defaultMessage: 'Timer trigger',
  },
});
