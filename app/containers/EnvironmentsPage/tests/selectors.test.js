// import { fromJS } from 'immutable';
// import { makeSelectEnvironmentsPageDomain } from '../selectors';

// const selector = makeSelectEnvironmentsPageDomain();

describe('makeSelectEnvironmentsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
